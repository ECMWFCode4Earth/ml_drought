from pathlib import Path


if __name__ == "__main__":
    # load static data
    #  load in the target data (nse, ∆nse)
    #  create X, Y training samples
    #  fit a random forest model
    #  check the accuracy of the fit
    #  produce feature importances (like linear coefficients)
    pass
